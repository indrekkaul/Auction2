import { TransactionEvaluation } from './transaction-evaluation';

describe('TransactionEvaluation', () => {
  it('should create an instance', () => {
    expect(new TransactionEvaluation()).toBeTruthy();
  });
});
